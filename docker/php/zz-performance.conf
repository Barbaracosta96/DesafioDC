[www]
; Dynamic workers — scales between min and max based on load
pm = dynamic
pm.max_children = 16
pm.start_servers = 4
pm.min_spare_servers = 2
pm.max_spare_servers = 6
pm.max_requests = 1000

; Request timeout
request_terminate_timeout = 60

; Status page for monitoring
pm.status_path = /fpm-status
